# Floors are the versions this app was verified against; ceilings stop a
# major release from breaking it. Deliberately ranges, not exact pins:
# streamlit pulls in pyarrow (a heavy C++ dependency), and over-constraining
# the resolver is how a deploy ends up compiling it from source instead of
# taking a prebuilt wheel — which on a small cloud instance means a build
# that runs for tens of minutes or runs out of memory.
pandas>=2.2,<3
streamlit>=1.61,<2
plotly>=6.9,<7
